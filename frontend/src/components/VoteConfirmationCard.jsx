import React from 'react';

const RestaurantCard = ({ place_id, name, photo_reference }) => {
    // Function to generate price level icons based on the integer value

    return (
        <div className='absolute top-0 left-0 flex justify-center items-center border-2 border-gray bg-gray rounded-lg h-[30vh] w-full p-3'>
            <h1> Name </h1>
        </div>
    );
};

export default RestaurantCard;
